const inputId = document.getElementById("inputId");
const inputPw = document.getElementById("inputPw");

function join(){
    const Id = document.getAnimations("inputId");
    const Pw = document.getAnimations("inputPw");
    (Id || Pw) ? msg.style.display
}
